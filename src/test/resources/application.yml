dgc:
  gateway:
    connector:
      enabled: true
      endpoint: https://dgcg.example.org
      proxy:
        enabled: false
        host:
        port: -1
      max-cache-age: 0
      #      tls-trust-store:
      #        password:
      #        path:
      tls-key-store:
        alias: 1
      #        password:
      #        path:
      trust-anchor:
        alias: ta_tst
      #        password:
      #        path:
      upload-key-store:
        alias: upload
      enable-ddcc-support: true
#        password:
#        path:
